{% extends "base.html.twig" %}

{# {% form_theme formView _self %} instade of _self courrent file location we can use "bootstrap_4_layout.html.twig" #}
{# {% form_theme formView "bootstrap_4_layout.html.twig" %} #}
{# _self means that the file is on same directory #}


	{% block title %}
Création d'un produit
{% endblock %}

	{% block body %}<h1>
	Nouveau Produit
	</h1>
	{# {{form(formView)}}  we can also breadking down form in three parts#}

	{# this is the same code like above but breaked down (décompose en frasais) #}
	{{ form_start(formView) }}

	{{ form_errors(formView) }}

	{# {{ form_widget(formView) }} form_widget we can breaking down into four parts #}

		<div class="row"> <div class="col">
			{{form_row(formView.name)}}
			{{form_row(formView.price)}}
		</div>

		<div class="col">
			{{form_row(formView.mainPicture)}}
			{{form_row(formView.category)}}
			{{form_row(formView.shortDescription)}}
		</div>

	</div>


	<button type="submit" class="btn btn-primary">
		<i class="fas fa-save mx-2"></i>Creer le produit
	</button>
	{{ form_end(formView) }}
{% endblock %}

{# form represent vormView.name , formView.price #}
{# {% block form_row %}
	<div class="form-group">
		{{form_label(form)}}
		{{form_widget(form, {"attr": {"class": "form-control"} }) }}
		{{form_help(form)}}
		{{form_errors(form)}}
	</div>
{% endblock %} #}
